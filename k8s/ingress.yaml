apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: conscious-bridge-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: conscious-bridge-service
            port:
              number: 80
