stages:
  - build

build-docker:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - echo "Starting Docker build..."
    - docker build -t test-image .
    - echo "Docker build completed successfully"
